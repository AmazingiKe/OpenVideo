import type { SubtitleDisplaySettings } from "@/shared/types";

export const DEFAULT_SUBTITLE_DISPLAY_SETTINGS: SubtitleDisplaySettings = {
  font_size: "medium",
  position: "bottom",
  background: "shadow",
};
